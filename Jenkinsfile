pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'hello-Clarusway_Way to Reinvent Yourself'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
